---
description: Actualizar conocimiento existente en CortexFS. Uso: /cortex-update [category] [id] [nuevo-contenido]
agent: build
---

Actualiza información existente en CortexFS:

```bash
cortex update "$1" "$2" "$3"
```

Parámetros:
- $1: categoría
- $2: identificador
- $3: nuevo contenido
